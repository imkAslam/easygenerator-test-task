export enum DepartmentName {
  LABS = 'lab',
  COLLECTION_CENTER = 'collection center',
  STORE = 'store',
}

export enum ItemType {
  BUNDLE = 'bundle',
  BOX = 'box',
  PACK = 'pack',
  SINGLE = 'single',
}
